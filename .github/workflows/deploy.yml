# Build the app and publish it to GitHub Pages on every push to main.
#
# One-time repo setup (Settings):
#   - Pages -> Source: "GitHub Actions"
#   - Actions variable VITE_RELAY_URL pointing at your blind relay over wss,
#     e.g. wss://relay.example.org - until it is set, the build bakes in the
#     placeholder below and login will fail with a clear connection error.
name: deploy-web

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run build
        env:
          # Project sites are served from /<repo>/.
          VITE_BASE: /grademax/
          VITE_RELAY_URL: ${{ vars.VITE_RELAY_URL || 'wss://REPLACE-WITH-YOUR-RELAY-DOMAIN' }}
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
