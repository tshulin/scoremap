{
  "name": "grademax-relay",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Blind WebSocket<->TCP relay for grademax. Runs on the VPS; sees only ciphertext.",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node server.mjs",
    "test": "node --test --test-force-exit"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
