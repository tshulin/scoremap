# Local execution plan
execution-plan.md

# Reference clone of the original GradeCompass (has its own .git) — never commit
gradecompass/

# Secrets & personal data
.env
captures/
sample_credentials.md

# Build & tooling output
node_modules/
coverage/
